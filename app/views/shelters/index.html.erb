<h1>All Shelters</h1>

<% @shelters.each do |shelter| %>
  <p><%= shelter.name %></p><%= link_to "Edit #{shelter.name}", "/shelters/#{shelter.id}/edit" %>
  <%= button_to 'Delete', "/shelters/#{shelter.id}", method: :delete %>
  <br>
<% end %>
<br><br><br>
<%= link_to "New Shelter", '/shelters/new' %>
